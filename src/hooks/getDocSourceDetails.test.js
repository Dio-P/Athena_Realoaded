// import { renderHook } from '@testing-library/react';

describe('findName', () => {
  test('should return the correct name of the doc if source is paper ', () => {

  });
  test('should return the correct name of the doc if source is confluence ', () => {

  });
  test('should return the correct name of the doc if source is github , zeplin , jira or figma ', () => {

  });
});

describe('findSource', () => {
  test('should return the correct source if link has / in it', () => {

  });
  test('should return the correct source if link has only dots in it', () => {

  });
});

describe('seperateLinkParts', () => {
  test('should seperate the link on /', () => {

  });
  test('should filter out empty strings', () => {

  });
});

describe('getDocSourceDetails', () => {
  test('happy path name and sourece are correct', () => {

  });
  //   gemini...
  // import { renderHook } from '@testing-library/react-hooks';
  // import useDocSourceDetails from './useDocSourceDetails'; // Assuming the hook is in this file

  // describe('useDocSourceDetails', () => {
  //   it('should extract source and name for confluence link', () => {
  //     const confluenceLink = 'https://confluence.example.com/space+page+title';
  //     const { result } = renderHook(() => useDocSourceDetails(confluenceLink));

//     expect(result.current.source).toBe('confluence');
//     expect(result.current.name).toBe('space page title');
//   });
// });
});
