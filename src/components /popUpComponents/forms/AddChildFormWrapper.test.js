// import React, { render } from '@testing-library/react';
// import AddChildFormWrapper

describe('AddChildFormWrapper', () => {
  test('should have all the input types', () => {

  });

  test('should populate the tags dropdown with the right options', () => {

  });

  test('on clicking tag dropdown option should populate the tag value correctly', () => {

  });

  test('on clicking add should update the db with the right arguments', () => {

  });
});
